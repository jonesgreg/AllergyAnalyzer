<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jack.allergyanalyzer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".IngSearchActivity" />
        <activity android:name=".searchActivity" />
        <activity android:name=".SearchingActivity"/>
        <activity android:name=".RecipeListActivity"/>
        <activity android:name=".SavedProfilePage" />
        <activity android:name=".RecSearchActivity"></activity>
    </application>

</manifest>